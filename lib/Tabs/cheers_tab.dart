import 'package:flutter/material.dart';

class CheersTab extends StatelessWidget {
  const CheersTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('CHEERS TAB'),
      ),
    );
  }
}