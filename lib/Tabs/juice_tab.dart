import 'package:flutter/material.dart';

class JuiceTab extends StatelessWidget {
  const JuiceTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('JUICE TAB'),
      ),
    );
  }
}